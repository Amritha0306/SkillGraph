{
  "name": "skillgraph-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "SkillGraph — Job & Skill Recommendation Platform powered by CognoDB and openCypher",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm install --prefix backend && npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm install --prefix backend && npm run build --prefix backend",
    "start:backend": "npm run start --prefix backend",
    "seed": "npm run seed --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^7.0.2"
  }
}